defmodule Schema.Admin do
  use Absinthe.Schema


  query do
    end

    
    mutation do
    
    
 #     field :create_product, :products do 
  #    arg :ppl_id, non_null (:id) # to do move to context
   #   arg :shop_id, non_null (:id)
    #  arg :name, non_null(:string)
     # arg :price, non_null(:integer)
     # arg :amount, :integer
     # resolve &Resolvers.create_product/2
     # end





    end
  

 
end